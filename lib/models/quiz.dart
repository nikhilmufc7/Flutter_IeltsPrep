class Quiz {
  List question;
  Map options;
  List answers;
  String quizTitle;
  double indicatorValue;

  Quiz(
      {this.question,
      this.options,
      this.answers,
      this.quizTitle,
      this.indicatorValue});
}
