class Quiz {
  List question;
  List<dynamic> options;
  List answers;
  String quizTitle;
  double indicatorValue;

  Quiz(
      {this.question,
      this.options,
      this.answers,
      this.quizTitle,
      this.indicatorValue});
}
